import { V4Options } from "uuid"

export interface Irows{
    id: V4Options
    numberProduct:string
    name: string
    rating: string
    count: string
    date: string
    price: string
}
export interface Icolumns{
    id: number
    label:string
}